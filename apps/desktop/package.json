{
  "name": "@cowrangler/desktop",
  "version": "2.2.0",
  "private": true,
  "description": "Cowrangler Desktop — local-first Code and Design workspace.",
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "pack": "npm run build && electron-builder"
  },
  "dependencies": {
    "@cowrangler/adapter-code": "*",
    "@cowrangler/adapter-design": "*",
    "@cowrangler/core": "*",
    "@remotion/player": "^4.0.519",
    "electron": "^41.10.6",
    "remotion": "^4.0.519",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4"
  }
}
