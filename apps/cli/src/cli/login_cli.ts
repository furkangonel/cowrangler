/**
 * login_cli — devre dışı.
 *
 * Abonelik OAuth login akışı (oauth_subscriptions.ts) kaldırıldı; bu dosya
 * artık hiçbir yerden import edilmiyor. Sandbox'ta dosya silinemediği için
 * boş bırakıldı — güvenle elle silinebilir.
 */
export {};
